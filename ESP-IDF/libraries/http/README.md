# http Libraries (ESP-IDF)

Collection of HTTP-related components for ESP-IDF.

## Available Component

- `webServer`: lightweight route-based wrapper around `esp_http_server`.

See:

- `webServer/README.md`
